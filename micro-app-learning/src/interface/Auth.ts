export default interface Auth {
    username : string;
    password : string;
}

export interface IRegister {
    fullname: string;
    username: string;
    password: string;
    address: string;
    gender: string;
}